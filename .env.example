# Copy this file to .env and fill in real values.
# .env is gitignored and must never be committed.

# Postgres connection string
DATABASE_URL=postgres://keeper:keeper@localhost:5432/keeper

# Kafka brokers (comma-separated)
KAFKA_BROKERS=localhost:9092

# Sui network
SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# Your Sui testnet wallet address
SUI_SIGNER_ADDR=0x

# Your Sui private key as hex (32-byte seed or 64-byte full key)
# Generate with: sui keytool generate ed25519
# Export with: sui keytool export --key-identity <alias>
SUI_PRIVATE_KEY=

# Keeper Network on-chain objects (populate after deploying keeper/move)
# Published package ID from: sui client publish keeper/move --gas-budget 100000000
KEEPER_REGISTRY_PKG=0x
# Registry shared object ID (printed in publish output or visible on explorer)
KEEPER_REGISTRY_ID=0x
# Your KeeperCredential owned object ID
# Create with: sui client call --package $KEEPER_REGISTRY_PKG --module credential --function register ...
KEEPER_CREDENTIAL_ID=0x